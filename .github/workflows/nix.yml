name: Nix
on:
  pull_request:
  push:
    branches:
      - dev
jobs:
  nix-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: cachix/install-nix-action@v31

      - name: Nix flake check
        run: nix flake check
